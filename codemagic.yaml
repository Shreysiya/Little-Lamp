workflows:
  build-apk:
    name: Build APK
    environment:
      flutter: stable
    scripts:
      - unzip *.zip
      - cd */
      - flutter pub get
      - flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/*.apk
